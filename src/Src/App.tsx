import React from 'react';
import ReactDOM from 'react-dom/client';
import VisionRoomGame from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VisionRoomGame />
  </React.StrictMode>
);
